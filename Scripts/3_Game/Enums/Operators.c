enum ENewStoryGroupOperators
{
    // Выбирает минимальное значение из текущего и входящего
    LOWEST,
    // Выбирает максимальное значение из текущего и входящего
    HIGHEST,
    // Оператор '+'
    ADD,
    // Оператор '-'
    SUBSTRACT,
    // Оператор '*'
    MULTIPLICATIVE,
    // Оператор '/'
    DIVISION,
    // Заменяет текущее значение
    SET
}
